<Window x:Class="DbToolGui.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Path=Title}" Height="600" Width="800"
        Icon="{Binding Path=Icon}">
    <Grid>
        <DockPanel LastChildFill="True">
            <DockPanel Name="TopPanel" DockPanel.Dock="Top" LastChildFill="True">
                <Button Name="ConnectButton" Content="{Binding Path=ConnectCommandName, Mode=OneWay}" Command="{Binding Path=ConnectCommand}" DockPanel.Dock="Right" />
                <ComboBox Name="ConnectionBox"
                          ItemsSource="{Binding Path=AvailableConnections, Mode=OneWay}"
                          SelectedItem="{Binding Path=SelectedConnection, Mode=TwoWay}">
                </ComboBox>
            </DockPanel>
            <DockPanel Name="StatusPanel" DockPanel.Dock="Bottom">
                <TextBox Name="StatusTextBox"
                         IsReadOnly="True"
                         HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto"
                         AcceptsReturn="True" AcceptsTab="true"
                         Text="{Binding Path=StatusText}"
                         DockPanel.Dock="Bottom" >
                </TextBox>
                
                
            </DockPanel>
            <DockPanel LastChildFill="True">
                <TextBox Name="ResultTable" DockPanel.Dock="Bottom"
                         IsReadOnly="True"
                         HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                         AcceptsReturn="True" AcceptsTab="true"
                         Text="{Binding Path=QueryResult, Mode=OneWay}"
                         />
                <Button Name="ExecuteButton" Content="Execute" DockPanel.Dock="Top" Command="{Binding Path=ExecuteCommand}" />
                <RichTextBox Name="EditorBox"
                         FontFamily="Courier New" FontSize="12"
                         AcceptsReturn="True" AcceptsTab="true"
                         VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                         TextChanged="EditorBox_TextChanged"
                         KeyUp="EditorBox_KeyUp">
                    <FlowDocument Name="EditorDocument">
                        
                    </FlowDocument>
                </RichTextBox>
            </DockPanel>
        </DockPanel>
    </Grid>
</Window>
