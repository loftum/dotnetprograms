<UserControl x:Class="HourGlass.Views.WeekControl2"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="700">
    <DockPanel>
        <Grid Name="DetailsGrid" DockPanel.Dock="Top">
            <Grid.RowDefinitions>
                <RowDefinition Height="24" />
                <RowDefinition Height="24" />
                <RowDefinition Height="24" />
            </Grid.RowDefinitions>
            <Label Grid.Row="0" Content="Week info" />
            <TextBox Grid.Row="1" IsReadOnly="True"  Name="WeekNameBox" Text="{Binding Path=Number, Mode=OneWay}" />
            <Button Grid.Row="2" Name="AddUsageButton" Content="Add Code" Command="{Binding AddUsageCommand}" />
        </Grid>
        <DataGrid AutoGenerateColumns="False" Height="200" Width="200"
                  HorizontalAlignment="Left" 
                  Name="WeekGrid" VerticalAlignment="Top" />
    </DockPanel>
</UserControl>
